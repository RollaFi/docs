---
title: 'List Transactions'
openapi: 'GET /transactions'
---

Retrieve a paginated list of your transactions, newest first, with optional filtering by status and date range.

## Example Request

```bash
curl -X GET "https://api.rolla.xyz/api/v1/external/transactions?page=1&pageSize=20&status=completed" \
  -H "X-API-Key: your_api_key_here"
```

## Query Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `page` | integer | Page number, starting at 1 (default: 1) |
| `pageSize` | integer | Results per page (default: 20) |
| `status` | string | Filter by status: `pending`, `processing`, `completed`, `sent`, `failed`, `rejected`, `refunded`, `negotiating`, `cancelled` |
| `startDate` | string | Inclusive start, `YYYY-MM-DD` or a full ISO 8601 timestamp |
| `endDate` | string | Inclusive end, `YYYY-MM-DD` or a full ISO 8601 timestamp |

<Note>
  This endpoint filters on status and date only. To also filter by currency, type or reference,
  use [List Wallet Transactions](/api-reference/endpoint/wallet/transactions), which reads the
  same transactions.
</Note>

Transactions with status `failed` are left out unless you ask for them with `status=failed`.

## Example Response

```json
{
  "status": 200,
  "message": "Transactions retrieved successfully",
  "success": true,
  "data": {
    "transactions": [
      {
        "id": "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
        "transaction_type": "withdrawal",
        "status": "completed",
        "description": "Vendor payment",
        "external_reference": "ref-abc123",
        "fee_amount": 53,
        "source_amount": 500000,
        "destination_amount": 499947,
        "source_currency": "NGN",
        "destination_currency": "NGN",
        "created_at": "2024-01-15T10:30:00.000Z",
        "updated_at": "2024-01-15T10:30:45.000Z"
      },
      {
        "id": "5f1c9d0a-2b73-4e86-9a5c-3d8f1e6b0c42",
        "transaction_type": "deposit",
        "status": "completed",
        "description": "Deposit from Acme Trading Ltd",
        "external_reference": "US-DEP-7K4M2B",
        "fee_amount": 0,
        "source_amount": 2500000,
        "destination_amount": 2500000,
        "source_currency": "USD",
        "destination_currency": "USD",
        "payment_rail": "wire_international",
        "virtual_account": {
          "id": "9c4e1f77-3a25-4d80-b6f1-0e7a2b5c8d93",
          "currency": "USD",
          "method": "bank_transfer",
          "bank_name": "Lead Bank",
          "account_number": "9876543210",
          "account_name": "Acme Ltd / Rolla",
          "routing_number": "021000021",
          "swift_code": "LEADUS33"
        },
        "payer": {
          "name": "Acme Trading Ltd",
          "account_number": "5551234567",
          "bank_name": "HSBC",
          "country": "Hong Kong"
        },
        "created_at": "2024-01-15T11:02:00.000Z",
        "updated_at": "2024-01-15T11:02:10.000Z"
      }
    ],
    "totalCount": 150,
    "page": 1,
    "pageSize": 20,
    "totalPages": 8
  }
}
```

Amounts are in **minor units** — `500000` is ₦5,000.00. Fields that do not apply to a
transaction are **omitted** rather than returned as `null`.

## Payment tracking references

A cross-border payout carries `uetr` and `tracking_codes` once the SWIFT network returns
them — the references a beneficiary's bank is chased with.

```json
{
  "id": "9a56d01f-dc72-4ace-bbbc-a237bdb1c599",
  "transaction_type": "withdrawal",
  "status": "sent",
  "uetr": "97ed4827-7b6f-4491-a06f-b548d5a7512d",
  "tracking_codes": {
    "UETR": "97ed4827-7b6f-4491-a06f-b548d5a7512d",
    "IMAD": "20260818B1QGC01C000123"
  }
}
```

| Field | Description |
|-------|-------------|
| `uetr` | The payout's **UETR** (Unique End-to-end Transaction Reference) — the UUID that identifies the payment across every bank that touches it. Quote it when tracing or recalling a payment. |
| `tracking_codes` | Every reference recorded against the payout, keyed by name — the `UETR`, plus `IMAD` on corridors that produce one. Read `uetr` for the UETR; use this if you also surface the others. |

<Warning>
  **Both fields are omitted until the reference exists** — they are not returned as
  `null`. The UETR is assigned by the correspondent network *after* the payout is sent,
  typically within minutes but sometimes hours later, so a payout read back immediately
  after you create it will not carry one. Local-rail and stablecoin payouts never get one.
  Branch on the field's presence; never block your flow waiting for it.
</Warning>

<Tip>
  To be told the moment a UETR lands rather than polling for it, subscribe to the
  [`transaction.uetr.assigned`](/api-reference/webhooks/events#payout-tracking-events)
  webhook. It carries the same value.
</Tip>

## Identifying the transfer method on deposits

Incoming deposits carry a `payment_rail` field saying how the money arrived, so you can reconcile per channel rather than parsing the description.

| Value | Meaning |
|-------|---------|
| `ach` | US domestic ACH |
| `wire_domestic` | US domestic wire (Fedwire) |
| `wire_international` | International wire — this is the **SWIFT** case |
| `mobile_money` | Mobile money collection |

There is no `swift` value; an international wire **is** the SWIFT case. The field is **omitted** (not `null`) when the provider does not report a transfer method, and never appears on payouts or crypto deposits — treat its absence as "rail unknown".

The same field is delivered on the [deposit webhook](/api-reference/webhooks/payloads#which-rail-a-deposit-arrived-on).

<Tip>
  Use date range filters to export transaction data for specific periods. Combine with status filter to find failed transactions that may need to be retried.
</Tip>
